name: Source checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  source-check:
    runs-on: windows-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4
      - name: Run lightweight source checks
        shell: pwsh
        run: .\check-source.ps1
