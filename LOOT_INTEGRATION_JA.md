# 1.16ルート計算の統合記録

## 現在の対応範囲

Java 1.16.1 / 1.16.5の砂漠の寺院、難破船、埋もれた宝、荒廃したポータルの
チェスト計算を実装しました。Seed Atlasの検索スレッド内でCの計算を直接呼ぶ
ため、外部のJavaプロセスやMineMapの起動は不要です。

結果はアイテムごとの合計個数です。チェスト内のスロット位置は扱いません。
砂漠の寺院のエンチャント本は、Java 1.16.xで抽選対象になる37種類について
種類とレベル1～5の内訳も保持します。荒廃したポータルの金装備と難破船の
革装備は、後続アイテムの乱数を正しく合わせるためエンチャント抽選も再現
しますが、現GUIでは装備のエンチャント内容自体は条件にしません。

## 参照した固定バージョン

作業フォルダー直下に、確認時点の参照ソースを固定してあります。

| 参照物 | バージョン / コミット |
|---|---|
| MineMap | 1.0.26の配布JAR、ソース`28a643bcc2881fc0d8cceb83ece07abeef6a920d` |
| SeedFinding mc_feature_java | 1.171.1、`700d15fc65653c83cdffb00a98f143e6bba7a260` |
| SeedFinding mc_core_java | 1.192.1、`173a507ca33f33d081c575352bec335ee507c3c1` |
| SeedFinding mc_seed_java | 1.171.1、`a7543ead9132421b19c4a4d5e17219a8c309d929` |
| SeedFinding mc_math_java | 1.171.0、`4c59c2ebdd41d12c6322d21d5f4b4427e6d2107f` |

MineMap自身のLootクラスは、ルートテーブルを再実装せずSeedFindingのJava
ライブラリを呼び出しています。そのため、C移植の基準はMineMapの画面表示だけ
ではなく、上記の正確な依存バージョンの処理としました。

## 計算手順

砂漠の寺院のチャンク座標を`chunkX, chunkZ`とすると、1.16では次の順です。

1. ワールドSeedと`chunkX * 16, chunkZ * 16`からpopulation seedを生成
2. decoration salt `40003`を加えてJava Randomを初期化
3. 対象チェストの内部RNG index（0～3）に応じて乱数を進める
4. `nextLong()`を各チェストのloot seedとして使用
5. 1個目のloot poolを2～4回、2個目を4回抽選して個数を合計

`getDesertPyramidLoot(x, z [, chest])`の`chest`はLua向けに1～4です。
これは物理的な北・南・東・西ではなく、Vanilla内部のRNG順です。参照Javaの
砂漠の寺院generatorにも実座標対応のFIXMEが残っているため、確認できるまでは
方角名を付けません。

追加した3構造物では、固定したSeedFinding / MineMapの処理に合わせて次も
再現しています。

- 埋もれた宝: decoration salt `30001`と1個のチェスト
- 荒廃したポータル: decoration salt `40005`と1個のチェスト
- 難破船: decoration salt `40006`、浜辺型 / 海中型、テンプレート抽選、
  回転、チェストの実ブロック座標、同じチャンクに属するチェスト数による
  RNG進行

難破船のGUI上のチェスト番号は物理的な生成順ではなく、ルートテーブルの種類
である`物資`、`地図`、`宝物`です。船のテンプレートによっては1種類または
2種類しか存在しません。

## 一致確認用の既知値

MineMap 1.0.26と固定したSeedFinding依存ライブラリから、次の値を生成しました。

- world seed: `3515201313347228787`
- 砂漠の寺院のchunk: `(17, -9)`
- Minecraft: Java 1.16.1

| RNG index | 主な結果（0個は省略） |
|---:|---|
| 0 | spider_eye 3, rotten_flesh 9, gunpowder 8, sand 1 |
| 1 | emerald 1, bone 5, saddle 1, iron_horse_armor 1, enchanted_golden_apple 1, sand 9 |
| 2 | gold_ingot 3, bone 15, spider_eye 1, string 1, sand 4 |
| 3 | diamond 1, emerald 2, bone 20, rotten_flesh 6, enchanted_book 1, gunpowder 4 |

この値を`cubiomes/tests_versions.c`の回帰テストへ固定しました。Cコードとは別に
Java Randomを再現した検算でも4チェストすべてが一致しています。
RNG index 3のエンチャント本が`silk_touch I`であることも固定しています。

追加した3構造物も、MineMap 1.0.26の固定依存ライブラリから得た次の値を
回帰テストへ固定しています。

| 構造物 | world seed / chunk | 一致確認した主な結果 |
|---|---|---|
| 埋もれた宝 | `123`, `(56,-105)` | 海洋の心1、鉄5、金6、焼き鱈7 |
| 荒廃したポータル | `239648`, `(4,7)` | 時計1、火打石と打ち金1、金の剣1、金のレギンス1 |
| 難破船 | `2276366175191987160`, `(-31,-32)` | 3種類のチェスト、テンプレート、180度回転、チェスト位置と全アイテム |

## GUIで選択できる条件

4種類の構造物条件の生成バリアント欄に`チェスト内容の条件`を追加しました。
また、Condition typeのOtherに`範囲内の構造物Loot合計`を追加しました。
現在は次を区別できます。

- 砂漠の寺院の特定チェスト1～4
- 難破船の物資 / 地図 / 宝物チェスト
- どれかのチェスト、全チェストの合計、存在する各チェストすべて
- 1種類の最小個数・最大個数・完全一致
- アイテムを含む / 含まない
- 同じチェスト内に複数アイテムが揃う
- 複数チェスト全体で複数アイテムが揃う
- 複数構造物のいずれか、各構造物それぞれ、全構造物の合計
- 複数のアイテム条件をANDまたはORで評価
- エンチャント本の種類とレベル範囲

アイテム条件の行は動的に追加・削除でき、固定個数の上限はありません。
セッション等から壊れたデータを読んだ際のメモリ保護として、デコード時だけ
10万行を安全上の上限にしています。

Luaの`getDesertPyramidLoot`も従来どおり利用でき、各チェストと`total`に
`enchanted_books.<enchantment>[level]`および
`enchanted_books.<enchantment>.total`を追加しています。

## 制限と次の候補

荒廃したポータルは、生成時の地形・溶岩などによってチェストブロックが最終的に
残らない例までは判定しません。現条件はMineMapと同じく「その構造物のチェストが
配置される場合の中身」を計算します。

ソースにはエンドシティ用ルートもあります。ただし、各構造物はチェストごとの
loot seedを得るまでの生成順やテンプレート配置が異なるため、既存テーブルの
単純な差し替えではなく、構造生成を含めて確認してから追加します。

1.20以降はルートテーブルや乱数源の変更をバージョンごとに再確認してから
別実装として追加します。現APIは未対応バージョンで`nil`を返すため、誤った
1.16結果を表示しません。

## 48-bit検索での事前判定と再利用

`Loot高速化（48-bit検索）`を有効にした場合、`PASS_FULL_48`で
`getStructurePos`の候補座標に対するLootを先に調べます。
`48-bit only`では、この事前判定を満たし得る構造Seedだけを出力します。
`48-bit family blocks`では、各検索workerがルールごと・構造物座標ごとの
必要アイテム数をキャッシュし、上位16bitだけが変わった時はLootテーブルを
再抽選しません。

Java Randomの初期化は48bitへマスクされるため、砂漠の寺院、埋もれた宝、
荒廃したポータルのLootはこの再利用で厳密に同一です。難破船は上位16bitで
決まるバイオームから浜辺型／海中型が変わり得るため、その2分類を別キーに
しています。48bit段階では両方を計算し、どちらかが一致し得れば候補を残します。
FULL_64では実際のバイオーム側だけを確定採用します。

構造物のviability、バイオーム、生成バリアント、範囲内で有効な構造物集合は
キャッシュしません。`INSTANCE_TOTAL`も各64-bit Seedで有効な構造物だけを
改めて合算するため、現在対応しているLootでは検索結果の正確性を維持します。

FULL_48の`INSTANCE_EVERY`は、Loot不一致候補が上位16bitで非生成になる可能性を
考慮し、一致し得る候補数が必要数未満の場合だけ棄却します。
`INSTANCE_TOTAL`は、候補ごとの可能な個数から保守的な合計下限・上限を作り、
指定範囲と交差しない場合だけ棄却します。これにより48-bit onlyでもLoot条件を
反映しつつ、有効なfamilyを早期に落とす偽陰性を防ぎます。
